# מפרט פרויקט מסכם — סוכן AI לבעיה מהתעשייה / ממערכות מידע

> מסמך זה הוא הרחבה מפורטת של סעיף 6 בסילבוס, מיועד להימסר לסטודנטים כחלק מהנחיות הפרויקט.

---

## 1. הרעיון המרכזי

כל קבוצה (4 סטודנטים, גמישות ל-3/5) בוחרת **בעיה אמיתית — או תרחיש מציאותי — מעולם התעשייה והניהול או מערכות מידע**, ובונה עבורה **סוכן מבוסס LLM (Agent)** שנותן לה מענה. הפרויקט הוא ההזדמנות ליישם את כל מה שנלמד בקורס על בעיה מהתחום שלכם, ולא על "עוד chatbot" גנרי.

**מה זה "מענה agentic" (ולא סתם קריאה בודדת ל-LLM)?**
סוכן שמבצע החלטות ופעולות **רב-שלביות**: קורא מידע (קובץ / API / מסד נתונים לדוגמה), מחליט מה לעשות בעזרת "כלים" (tools), מבצע פעולה, בודק את התוצאה וממשיך בהתאם. זהו בדיוק הרעיון שנלמד במפגש 6 (Agentic Coding) — הפרויקט הוא היישום שלו על בעיה אמיתית.

**היקף מינימלי סביר:** סוכן אחד עם 2-4 "כלים" מוגדרים היטב, שפותר משימה תפעולית/ניהולית אחת מוגדרת. **לא** נדרשת מערכת multi-agent, ולא נדרש pipeline של אימון מודל — זה שייך לתחומים אחרים (Data Science/ML), לא לקורס הזה.

---

## 2. דוגמאות לתחומי בעיה

הרשימה הבאה היא **דוגמאות בלבד** לעידוד חשיבה — לא רשימה סגורה. מוזמנים להביא בעיה מהתמחות אישית, סטאז', או פרויקט קודם:

- סוכן תזמון/הקצאת משאבים (scheduling assistant)
- סוכן ניתוח דוחות בקרת איכות — מזהה מגמות בתקלות, מציע כיווני שורש-בעיה
- סוכן ליישוב פערים בין הזמנות רכש לחשבוניות (matching/reconciliation)
- סוכן תיעוד תהליכים — הופך רשומות פגישה/ראיונות לפורמט SOP מסודר
- סוכן טריאז' לפניות שירות לקוחות או IT helpdesk
- סוכן שאילתות בשפה טבעית מול מסד נתונים לדוגמה (ERP/CRM מדומה)
- סוכן סיכום פגישות והפקת action items עם אחראים ותאריכי יעד
- סוכן ניתוח והמלצות למלאי/שרשרת אספקה

---

## 3. נתונים

**לא נדרשים נתונים אמיתיים/קנייניים מחברה.** קשה ולא ריאלי לצפות שקבוצה תשיג גישה לנתוני חברה אמיתיים תוך סמסטר. מותר ומומלץ באחת מהדרכים הבאות:

- נתונים ציבוריים (data.gov.il, Kaggle, מאגרים פתוחים אחרים).
- נתונים סינתטיים שהקבוצה יוצרת — אפשר וכדאי גם בעזרת כלי AI!
- תרחיש היפותטי-אך-מציאותי מהתחום, עם נתוני דוגמה שהקבוצה בונה בעצמה.

---

## 4. מודל ה-LLM וה-API

עובדים עם **API חינמי** — אין תקציב לפרויקט. הטבלה הבאה משקפת את המצב **נכון ליולי 2026** — התחום משתנה כל חודש, **יש לבדוק תנאים עדכניים בזמן הבחירה בפועל**, וזה עצמו חלק מהתרגול (בדיקת "מה עדיין נכון עכשיו" ולא הסתמכות על מדריך ישן).

| ספק | הערות (יולי 2026) |
|---|---|
| **OpenRouter** | מודלים מסומנים `:free`, מגבלת קצב (כ-20 בקשות/דקה, 50/יום ללא רכישת קרדיט). המגוון הרחב ביותר של מודלים תחת מפתח API אחד. |
| **NVIDIA NIM** (build.nvidia.com) | מארח עשרות מודלים פתוחי-משקל בחינם, ללא צורך בכרטיס אשראי (עשוי לדרוש אימות טלפון). |
| **Groq** | מהיר במיוחד, מודלים פתוחי-משקל (Llama וכו'), מגבלת קצב נדיבה יחסית, ללא כרטיס אשראי. |
| **Google AI Studio (Gemini API)** | חלון הקשר ענק (עד מיליון טוקן), מגבלות קצב נמוכות יחסית ליום. ⚠️ בטיר החינמי הנתונים עשויים לשמש לאימון מודלים של גוגל — לא להזין מידע רגיש/אמיתי של חברה. |

**כללים חשובים:**
- **כל קבוצה יוצרת חשבון/מפתח API משלה.** אין לשתף מפתח אחד בין קבוצות — עם 18 קבוצות במקביל, מפתח משותף יתמוטט ממגבלת הקצב תוך דקות.
- מותר ומעודד **לחפש ספקים נוספים** מעבר לרשימה — זה תרגול לגיטימי של "בדיקת המצב העדכני", לא סטייה מההנחיות.
- **בדיקה מוקדמת (לפני ה-checkpoint אחרי מפגש 6):** יש לוודא שהמודל שנבחר תומך ב-**function/tool calling** באופן אמין — לא כל המודלים החינמיים תומכים בכך באותה רמה. זו בדיוק המשימה שמשולבת בתרגול מפגש 6.

---

## 5. סינרגיה עם תוכן הקורס (הערה למרצה — לשלב בהוראה בפועל)

- **מפגש 6 (Agentic Coding):** נקודת ההתחלה בפועל של הפרויקט — בחירת API ווידוא תמיכה ב-tool calling.
- **מפגש 10 (אבטחת מידע):** ניהול מפתח ה-API בבטחה (לא ב-git, קובץ `.env`) הופך לדוגמה אישית ומוחשית, ולא רק תיאורטית — בתנאי שהקבוצות כבר עובדות עם מפתח אמיתי עד לשלב הזה.
- **מפגש 12 (אתיקה):** אוטומציה של תהליך עסקי/ניהולי בעזרת agent היא נקודת מוצא טבעית לדיון — מה קורה לתפקידים אנושיים בתהליך, שקיפות מול עובדים/לקוחות שמקיימים אינטראקציה עם הסוכן.

---

## 6. גבולות היקף (Guardrails) — כדי לא "להיתקע" בפרויקט רחב מדי

- סוכן **אחד**, לא ריבוי-סוכנים (multi-agent) — אלא כהרחבת-בונוס לקבוצות שמסיימות מוקדם.
- **2-4 כלים** (tools) מוגדרים היטב לכל היותר, לא יותר.
- **משימה תפעולית/ניהולית אחת מוגדרת** — לא "פלטפורמה" רב-תכליתית.
- עדיף פתרון צר שעובד היטב על "כלי" אחד, מאשר פתרון רחב שעובד חלקית.
